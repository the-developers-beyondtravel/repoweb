{% extends 'base-back.html.twig' %}

{% block title %}les Vols ajoutés {% endblock %}

{% block body %}
    <h1> <font face="verdana" size="20px" color="#FF7A59">vols</font> </h1>


    <div class="adv-table">
    <table  cellpadding="0" cellspacing="0" border="0" class="display table table-bordered" id="hidden-table-info">

        <font face="verdana" size="20px" >
        <thead>
            <tr>
                <th>Id</th>
                <td class="hidden-phone">  {{ vol.id }}</td>
            </tr>
          <tr>
                <th>Destination_aller</th>
                <td>{{ vol.destinationAller }}</td>
            </tr>
            <tr>
                <th>Destination_retour</th>
                <td>{{ vol.destinationRetour }}</td>
            </tr>
            <tr>
                <th>Voyage</th>
                <td>{{ vol.voyage }}</td>
            </tr>
            <tr>
                <th>Date_depart</th>
                <td>{{ vol.dateDepart ? vol.dateDepart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_retour</th>
                <td>{{ vol.dateRetour ? vol.dateRetour|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Passagers</th>
                <td>{{ vol.passagers }}</td>
            </tr>
            <tr>
                <th>Cabine</th>
                <td>{{ vol.cabine }}</td>

            </tr>
            </thead>



        <td>


        </td>
        </font> </tr>

    <tr>
     <td>
         <a href="{{ path('app_vols_edit', {'id': vol.id}) }}">
             <button class="btn btn-round btn-success btn-xs"><i class="fa fa-edit "></i></button> </a>
         </a>
     </td>
        <td>
            <a href="{{ path('app_vols_index') }}">
                <button class="btn btn-round btn-success btn-xs"><i class="fa fa-arrow-circle-left "></i></button> </a>

        </td>

        <td>
            {{ include('vols/_delete_form.html.twig') }}
        </td>

    </tr>



</table>
    </div>
    <!-- page end-->

{% endblock %}
