{% extends 'base-back.html.twig' %}

{% block title %}Vols index{% endblock %}

{% block body %}
    <h1><font face="verdana" size="20px" color="#DD5526">Vols index</font></h1>
    <div id="divs">
        <input class="form-control" type="text" id="search" placeholder="🔍">
    </div>

    <table class="table" id="t" >
        <div class="row mt">
            <div class="col-md-12">

                    <table class="table table-striped table-advance table-hover">

                        <h4><i class="fa fa-angle-right"></i> Vols disponibles</h4>
                        <hr>
        <thead>

            <tr>

                <th><i class=" fa fa-edit"></i> Id</th>
                <th>Destination_aller</th>
                <th>Destination_retour</th>
                <th>Voyage</th>
                <th>Date_depart</th>
                <th>Date_retour</th>
                <th>Passagers</th>
                <th>Cabine</th>
                    <th>actions</th>
            </tr>

        </thead>

                        <tbody id="all">
        {% for vol in vols %}

            <tr>
                <td>{{ vol.id }}</td>
                <td>{{ vol.destinationAller}}</td>
                <td>{{ vol.destinationRetour }}</td>
                <td>{{ vol.voyage }}</td>
                <td>{{ vol.dateDepart ? vol.dateDepart|date('Y-m-d') : '' }}</td>
                <td>{{ vol.dateRetour ? vol.dateRetour|date('Y-m-d') : '' }}</td>
                <td>{{ vol.passagers }}</td>
                <td>{{ vol.cabine }}</td>
                <td>
                    <a href="{{ path('app_vols_show', {'id': vol.id}) }}">
                    <button type="button" class="btn btn-round btn-warning">afficher</button></a>


                    <a href="{{ path('app_vols_edit', {'id': vol.id}) }}">
                    <button class="btn btn-success btn-xs"><i class="fa fa-pencil"></i></button></a>
                </td>
              <td>  {{ include('vols/_delete_form.html.twig') }}</td>

            </tr>



        {% else %}
        <tr>
            <td colspan="9">no records found</td>
        </tr>
                        {% endfor %}
                        <tbody id="search">
                        </tbody>
                    </table>
                </tbody>
            </table>
            </div>
                <!-- /content-panel -->
            </div>
            <!-- /col-md-12 -->
        </div>



    <a href="{{ path('app_vols_new') }}">
        <button type="button" class="btn btn-round btn-info">ajouter</button></a>

    <a href="{{ path('search_vols')}}" class="btn btn-round btn-info">recherce avancé</a>
    </center>



{% endblock %}
