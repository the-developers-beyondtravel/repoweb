{% extends 'base_back.html.twig' %}

{% block title %} modifier hotels {% endblock %}

{% block body %}

    <section id="main-content">
        <section class="wrapper">
            <h3><i class="fa fa-angle-right"></i>Modifier hotels</h3>



            {{ form_start (f,{attr:{novalidate:'novalidate'}}) }}


            <div class="row mt">
                <div class="col-lg-12">
                    <div class="form-panel">
                        <div class=" form">
                                <div class="form-group ">
                                    <label for="cname" class="control-label col-lg-2">Nom Hotel</label>
                                    {{ form_widget (f.nom) }}
                                    {{ form_errors (f.nom,{'attr':{'class':'text-danger'}}) }}
                                </div>


                                <div class="form-group ">
                                    <label for="cnbetoils" class="control-label col-lg-2">nombre d'étoiles </label>
                                    {{ form_widget (f.nbetoiles) }}
                                </div>
                                {{ form_errors (f.nbetoiles,{'attr':{'class':'text-danger'}}) }}
                                <div class="form-group ">
                                    <label for="curl" class="control-label col-lg-2">adresse</label>
                                    {{ form_widget (f.adresse) }}
                                </div>
                                {{ form_errors (f.adresse,{'attr':{'class':'text-danger'}}) }}
                                <div class="form-group ">
                                    <label for="ccomment" class="control-label col-lg-2">description </label>
                                    {{ form_widget (f.pointfort) }}
                                </div>
                                {{ form_errors (f.pointfort,{'attr':{'class':'text-danger'}}) }}

                                <div class="form-group last">
                                    <label class="control-label col-md-3">Image Upload</label>
                                    <div class="col-md-9">
                                        <div class="fileupload fileupload-new" data-provides="fileupload">

                                            <div>
                          <span class="btn btn-theme02 btn-file">
                            {{ form_widget (f.image) }}


                          </span>
                                                {{ form_errors (f.image,{'attr':{'class':'text-danger'}}) }}
                                            </div>
                                        </div>

                                    </div>
                                </div>
                                <div class="form-group">
                                  

                                    {{ form_widget (f.ajouter) }}

                                    {{form_end (f)}}
                                 </div>
                            </form>
                        </div>
                    </div>
                    <!-- /form-panel -->
                </div>
                <!-- /col-lg-12 -->
            </div>
            
            <!-- /row -->
            <!-- /row -->
        </section>
        <!-- /wrapper -->
    </section>
    {% endblock %}
  

