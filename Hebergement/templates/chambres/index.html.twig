
{% extends 'base_back.html.twig' %}

{% block title %} liste des chambres {% endblock %}

{% block body %}
<section id="main-content">
<section class="wrapper site-min-height">
        <div class="row">
          <div class="col-lg-12">

  {% for chambre in c %}

            <div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">
              <div class="custom-box">
                <div class="icn-main-container">
                  <img src="uploads/image/{{ chambre.getImage() }}" height="200" width="200">
                </div>
                <ul class="pricing">
                         <li>{{ chambre.typeChambre }}</li>
                        <li>nombre de lit  : {{ chambre.nbrLit }}</li>
                        <li>description : {{ chambre.description }}</li>
                        <li>prix par nuit : {{ chambre.prix }}</li>
                </ul>
                                <ul class="pricing">
                </ul>
                <a class="btn btn-success" href="{{ path('modifchambres',{'id':chambre.id}) }}">Modifier</a>
                <a class="btn btn-danger" href="{{ path('supp_Chambres',{'id':chambre.id}) }}">supprimer</a>
            
                         
              </div>
              <!-- end custombox -->
            </div>
            {% endfor %}
            <!-- end col-4 -->
  
            <!-- end col-4 -->
          </div>
          <!--  /col-lg-12 -->
        </div>
        <!--  /row -->
      </section>
      </section>
{% endblock %}
